<!--
this is the main settings screen.
it's a two-pane layout using constraintlayout, which is way more efficient.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface"> <!-- uses the dark background from our theme -->

    <!-- this is the header bar at the top -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appbar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:titleTextColor="?attr/colorOnSurface"
            app:title="@string/bloxstrap_settings" />

    </com.google.android.material.appbar.AppBarLayout>

    <!-- the side navigation menu on the left -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigation_rail"
        android:layout_width="120dp"
        android:layout_height="0dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/appbar"
        app:layout_constraintBottom_toTopOf="@+id/bottom_action_bar"
        app:headerLayout="@layout/nav_header"
        app:menu="@menu/navigation_menu" />
        <!--
        hey! you'll need to create a menu resource file named 'navigation_menu.xml'
        to define the items like 'integrations', 'about', etc.
        -->

    <!-- this is where the actual settings screens (fragments) will show up -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragment_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintStart_toEndOf="@id/navigation_rail"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/appbar"
        app:layout_constraintBottom_toTopOf="@+id/bottom_action_bar"
        tools:layout="@layout/fragment_integrations" /> <!-- example fragment -->

    <!-- a simple divider line -->
    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="?attr/colorPrimary"
        app:layout_constraintBottom_toTopOf="@+id/bottom_action_bar"/>

    <!-- this holds the save/close buttons at the bottom -->
    <LinearLayout
        android:id="@+id/bottom_action_bar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:padding="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- replaced the old fake buttons with real material buttons -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/close_button"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/close" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/save_button"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="@string/save" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/save_and_launch_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="@string/save_and_launch"
            app:backgroundTint="?attr/colorAccent" /> <!-- this will be purple! -->

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
