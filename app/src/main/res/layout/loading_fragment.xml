<?xml version="1.0" encoding="utf-8"?>
<!--
this thing holds the whole loading bar view.
decided to use a relative layout, it's just easier for this kinda stuff.
-->
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/colorPrimary"
    android:padding="16dp">

    <!-- i added a real progress bar at the top, looks way cooler -->
    <com.google.android.material.progressindicator.LinearProgressIndicator
        android:id="@+id/progress_indicator"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:indeterminate="true"
        android:layout_alignParentTop="true"
        app:indicatorColor="?attr/colorAccent"
        app:trackColor="?attr/colorPrimaryVariant" />

    <!-- just the bloxstrap logo -->
    <ImageView
        android:id="@+id/logo_image"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_below="@id/progress_indicator"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        android:layout_alignParentStart="true"
        android:src="@drawable/ic_bloxstrap_logo_small" />

    <!-- this linear layout just keeps the two text lines together -->
    <LinearLayout
        android:id="@+id/text_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/progress_indicator"
        android:layout_marginTop="16dp"
        android:layout_toEndOf="@id/logo_image"
        android:layout_toStartOf="@+id/cancel_button"
        android:orientation="vertical">

        <!-- the main text, like "applying flags" or whatever -->
        <TextView
            android:id="@+id/textview_taskcurrently"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceSubtitle1"
            android:textColor="?attr/colorOnPrimary"
            tools:text="Applying FastFlags..." />

        <!-- the smaller text below it for more deets -->
        <TextView
            android:id="@+id/textview_loadingstatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceBody2"
            android:textColor="?attr/colorOnPrimary"
            android:alpha="0.7"
            tools:text="Please wait a moment" />
    </LinearLayout>

    <!--
    a real button now, not that weird linear layout thing.
    the style makes it just text so it's not too in-your-face.
    -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/cancel_button"
        style="?attr/materialButtonOutlinedStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/progress_indicator"
        android:layout_marginTop="16dp"
        android:layout_alignParentEnd="true"
        android:text="@string/cancel" />

</RelativeLayout>
